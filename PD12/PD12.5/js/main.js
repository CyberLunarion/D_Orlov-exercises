const currentFishPopulation = 5;
const fishIncreasePerDay = 3;
const dayAmount = 3;

const finalFishPopulation = currentFishPopulation + (fishIncreasePerDay * dayAmount);

console.log('After',dayAmount,'days there will be',finalFishPopulation,'fish in the aquarium.');